# Fruit Recognition Frontend

Vite + React interface for the fruit recognition checkout flow.

## Run

```bash
npm install
npm run dev
```

The frontend expects the Flask API to be running at `http://127.0.0.1:3000`.
